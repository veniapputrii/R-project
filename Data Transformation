library(nycflight13)
library(tidyverse)
flights 
#shows the flightstable
glimpse(flights)
#shows all columns

#combining multiple verbs
flights |>
  filter(dest == "IAH") |>
  group_by(year,month, day) |>
  summarize(
    arr_delay = mean(arr_delay, na.rm= TRUE)
)

#filter the values
flights |>
  filter(dep_delay > 120) # only take the value where dep_delay is higher then 120
fligths |>
  arrange(year, month, day, dep_time)
